return {
    Array = require("bitsy.types.array"),
    Field = require("bitsy.types.field"),
    Struct = require("bitsy.types.struct"),
    Magic = require("bitsy.types.magic"),
    String = require("bitsy.types.string")
}
